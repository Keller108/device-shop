import './DeviceList.css';

export function DeviceList() {
    // let deviceConfig: DeviceEntityType | {} = {};
    // const { deviceStore } = useContext(deviceContext);

    return (
        <>
            {/* <h1 className="device-list__title">Samsung</h1> */}
            <ul className="device-list__devices">
                {/* <DeviceItem config={deviceConfig}/> */}
            </ul>
        </>
    )
}